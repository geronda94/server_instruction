sftp://goga@supercat.skin/  - подключение по ssh через проводник


Nginx #################################################################
sudo systemctl restart nginx - жесткий перезапускн nginx. Не стоит выполнять на рабочем сервере
sudo nginx -t     -тест синтаксиса, рекомендуется всегда выполнять перед перезагрузкой 
sudo systemctl reload nginx - мягкий перезапуск, не обрывает жеско все процессы пользователей а ждет их завершения
sudo nginx -s reload -две предыдущие команды одновременно выполняются в этой

systemctl
sudo systemctl is-enabled nginx -проверяет если сервис активируется при перезагрузке
sudo systemctl enable nginix - включает активацию при перезапуске системы
sudo systemctl disable nginx -  выключает активацию при перезапуске

ufw 
sudo ufw app list - покажет список разрешенных приложений
cd /etc/ufw/applications.d  - папка где хранятся конфигурации приложений контролируемых фаерволом
sudo ufw allow OpenSSH - Включаем необходимое для подлкючения по ssh приложение, без этого ни в коем случае не включать фаервол
sudo ufw delete allow Flask -забираем разрешение
sudo ufw allow 'Nginx Full' - Разрешаем работу nginx 
sudo ufw enable - добавляем фаервол в автозагрузку и перезапускаем ситему
sudo ufw disable - Убираем фаервол из  автозагрузки
sudo ufw reset - Сброс до заводских настроек
sudo ufw ... -dry-run проверяет ufw команду на наличие ошибок и если они есть то не запускает команду